<!DOCTYPE html>
<html lang="en">
<head>
  <title>Articles <%= title %></title>
  
  <link rel="stylesheet" href="/themes/<%= theme %>.css">
  <link rel="stylesheet" href="/css/style.css">

  <style>
  .article {
    background-color: #E9E9E9;
    padding: 10px;
    margin: 10px;
  }
  .img-fit {
    width: calc(100% + 20px);
    margin-left: -10px;
    margin-top: -10px;
    margin-bottom: 10px;
  }
  </style>

  <meta name="viewport" content="width=device-width, initial-scale=1.0">

</head>
<body>

  <% include ./partials/nav %>

  <div class="container">
    <div class="row">
      <div class="col-lg-4 col-sm-2"></div>
      <div class="col-lg-4 col-sm-8 col-xs-12">
        <% if( articles.length == 0 ) { %>
        <h1 class="text-center">No articles found <%= title %></h1>
        <% } else { %>
        <h1 class="text-center">Articles <%= title %></h1>
        <% for (var i = 0; i < articles.length; i++) { %>
        <div class="row article">
          <% if( articles[i].headerImg != "" ) { %>
          <img class="img-fit" src="/articleImg/<%= articles[i].id %>-<%= articles[i].headerImg %>" alt="<%= articles[i].urlTitle %>">
          <% } else { %>  
          <img class="img-fit" src="/imgs/noimg">
          <% } %>
          <div class="col-xs-8">
            <span>
              <b>
                <a target="_blank" href="/article/<%= articles[i].urlCategory %>/<%= articles[i].urlTitle %>"><%= articles[i].title %></a>
              </b>
            </span><br>
            <span><%= articles[i].datePublished %></span>
          </div>
          <div class="col-xs-4">
            <a target="_blank" href="/author/<%= articles[i].urlAuthor %>"><%= articles[i].author %></a>&nbsp;in
            <a target="_blank" href="/category/<%= articles[i].urlCategory %>"><%= articles[i].category %></a>
          </div>
        </div><br>
        <% } %>
        <% } %>
      </div>
    </div>
    <div class="col-lg-4 col-sm-2"></div>
  </div>

  <% include ./partials/footer %>

  <script src="/js/jquery.min.js"></script>
  <script src="/js/bootstrap.min.js"></script>
  <script src="/js/app.js"></script>

</body>
</html>